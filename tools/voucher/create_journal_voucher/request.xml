<ENVELOPE>
    <HEADER>
        <VERSION>1</VERSION>
        <TALLYREQUEST>Import</TALLYREQUEST>
        <TYPE>Data</TYPE>
        <ID>Vouchers</ID>
    </HEADER>
    <BODY>
        <DESC>
            <STATICVARIABLES>
                <SVCURRENTCOMPANY>{{ e .company_name }}</SVCURRENTCOMPANY>
            </STATICVARIABLES>
        </DESC>
        <DATA>
            <TALLYMESSAGE xmlns:UDF="TallyUDF">
                <VOUCHER VCHTYPE="Journal" ACTION="Create" OBJVIEW="Accounting Voucher View">
                    <DATE>{{ .date }}</DATE>
                    <VOUCHERTYPENAME>Journal</VOUCHERTYPENAME>
                    <REFERENCE>{{ e .reference }}</REFERENCE>
                    <NARRATION>{{ e .narration }}</NARRATION>

                    {{ range .lines }}
                    <ALLLEDGERENTRIES.LIST>
                        <LEDGERNAME>{{ e .ledger_name }}</LEDGERNAME>
                        <ISDEEMEDPOSITIVE>{{ if lt .amount 0.0 }}Yes{{ else }}No{{ end }}</ISDEEMEDPOSITIVE>
                        <AMOUNT>{{ .amount }}</AMOUNT>
                        {{ if .bill_allocations }}
                        {{ range .bill_allocations }}
                        <BILLALLOCATIONS.LIST>
                            <NAME>{{ e .name }}</NAME>
                            <BILLTYPE>{{ e .type }}</BILLTYPE>
                            <AMOUNT>{{ .amount }}</AMOUNT>
                        </BILLALLOCATIONS.LIST>
                        {{ end }}
                        {{ end }}
                    </ALLLEDGERENTRIES.LIST>
                    {{ end }}
                </VOUCHER>
            </TALLYMESSAGE>
        </DATA>
    </BODY>
</ENVELOPE>
