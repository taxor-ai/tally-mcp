<ENVELOPE>
    <HEADER>
        <VERSION>1</VERSION>
        <TALLYREQUEST>Import</TALLYREQUEST>
        <TYPE>Data</TYPE>
        <ID>Vouchers</ID>
    </HEADER>
    <BODY>
        <DESC>
            <STATICVARIABLES>
                <SVCURRENTCOMPANY>{{ e .company_name }}</SVCURRENTCOMPANY>
            </STATICVARIABLES>
        </DESC>
        <DATA>
            <TALLYMESSAGE xmlns:UDF="TallyUDF">
                <VOUCHER VCHTYPE="Sales" ACTION="Create" OBJVIEW="Accounting Voucher View">
                    <DATE>{{ .date }}</DATE>
                    <VOUCHERTYPENAME>Sales</VOUCHERTYPENAME>
                    <REFERENCE>{{ e .reference }}</REFERENCE>
                    <NARRATION>{{ e .narration }}</NARRATION>
                    <PARTYLEDGER>{{ e .debtor_ledger_name }}</PARTYLEDGER>

                    {{ range .lines }}
                    <ALLLEDGERENTRIES.LIST>
                        <LEDGERNAME>{{ e .ledger_name }}</LEDGERNAME>
                        <ISDEEMEDPOSITIVE>{{ if eq .entry_type "debit" }}Yes{{ else }}No{{ end }}</ISDEEMEDPOSITIVE>
                        <AMOUNT>{{ if eq .entry_type "debit" }}-{{ .amount }}{{ else }}{{ .amount }}{{ end }}</AMOUNT>
                        {{ if .bill_allocations }}
                        {{ range .bill_allocations }}
                        <BILLALLOCATIONS.LIST>
                            <NAME>{{ e .name }}</NAME>
                            <BILLTYPE>{{ e .type }}</BILLTYPE>
                            <AMOUNT>{{ .amount }}</AMOUNT>
                        </BILLALLOCATIONS.LIST>
                        {{ end }}
                        {{ end }}
                    </ALLLEDGERENTRIES.LIST>
                    {{ end }}
                </VOUCHER>
            </TALLYMESSAGE>
        </DATA>
    </BODY>
</ENVELOPE>
