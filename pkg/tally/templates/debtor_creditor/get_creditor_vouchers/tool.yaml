name: get_creditor_vouchers
description: Fetch all Journal vouchers for a specific creditor
implicit_params:
  - company_name
input_schema:
  properties:
    creditor_ledger_name:
      type: string
      description: Name of the creditor ledger to filter by (e.g., "ABC Suppliers")
  required:
    - creditor_ledger_name
