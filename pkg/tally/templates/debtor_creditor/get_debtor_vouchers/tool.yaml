name: get_debtor_vouchers
description: Fetch all Sales vouchers for a specific debtor
implicit_params:
  - company_name
input_schema:
  properties:
    debtor_ledger_name:
      type: string
      description: Name of the debtor/customer ledger (e.g., "TestStore")
  required:
    - debtor_ledger_name
